// Crie uma classe de Pessoa com todos os seus atributos:

using ClassPessoa;

String n, cpf;
int id;
Console.WriteLine("Nome: ");
n = Console.ReadLine();
Console.WriteLine("CPF: ");
cpf = Console.ReadLine();
Console.WriteLine("Idade: ");
id = int.Parse(Console.ReadLine());
/*Pessoa l1 = new Pessoa(n, cpf, id); // Só ta enviando os valores, quanto ela vale.
l1.apresentaInfoPessoa();*/
